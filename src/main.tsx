import React from 'react';
import ReactDOM from 'react-dom/client';
import PERTCalculator from './PERTEstimationCalculator';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PERTCalculator />
  </React.StrictMode>
);